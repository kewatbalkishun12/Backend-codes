* coding from BIjay

this is a backend code from scratch to production grade

if push doesn't work then config git with username and email and set origin to remote then add origin and finally push
$ git config --global user.email "email"
$ git config --global user.name "username"
git origin
then add git remote origin 
